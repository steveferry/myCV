<h4>Ecoles</h4>
<%= @cv.content.school.each do |cv|%>
	<%= cv.each do |content|%>
		<p>
		<%= content[1]["date"]%>
		<%= content[1]["date"]%>
		</p>
	<%end%>
<%end%>

<h4>Ecoles</h4>
<%= @cv.content.work.each do |cv|%>
	<%= cv.each do |content|%>
		<p>
		<%= content[1]["date"]%>
		<%= content[1]["date"]%>
		</p>
	<%end%>
<%end%>

<%= link_to 'Back', cvs_path%> - 
<%= link_to 'Edit', edit_cv_path(@cv)%>

